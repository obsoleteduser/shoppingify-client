import { createSlice } from "@reduxjs/toolkit";


const initialState = {
    product: [{name: '', id: '', quantity: 0, bought: '', }]
}

const currentlistSlice = createSlice({
    name: 'currentSlice',
   
})